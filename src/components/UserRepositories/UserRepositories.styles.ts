import styled from "styled-components";

export const UserRepositories = styled.section`
  padding-top: var(--gap-lg);
`;

export const UserRepositoriesTitle = styled.h2`
  margin-bottom: var(--gap-lg);
  color: white;
  text-align: center;
`;

export const SearchHistoryAdvise = styled.span`
  color: white;
  text-align: center;
  opacity: 0.75;
`;
